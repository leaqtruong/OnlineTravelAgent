const fs = require('fs'); const path = require('path'); function walk(dir, fileList = []) { const files = fs.readdirSync(dir); for (const file of files) { const stat = fs.statSync(path.join(dir, file)); if (stat.isDirectory()) { walk(path.join(dir, file), fileList); } else if (file.endsWith('.dart')) { fileList.push(path.join(dir, file)); } } return fileList; } const allFiles = walk('./lib'); const contents = allFiles.map(f => ({ path: f, name: path.basename(f), content: fs.readFileSync(f, 'utf8') })); for (const file of contents) { if (file.name === 'main.dart') continue; let isImported = false; for (const other of contents) { if (other.path === file.path) continue; if (other.content.includes(file.name)) { isImported = true; break; } } if (!isImported) { console.log('Dead file:', file.path); } }
